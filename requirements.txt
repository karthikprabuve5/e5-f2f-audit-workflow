# Runtime dependencies for e5-f2f-audit (mirrors e5-f2f-audit/pyproject.toml).
# Requires Python >= 3.12.
# For development, install the package instead:  pip install -e "e5-f2f-audit[dev]"
deepagents>=0.6.12
langchain-aws>=1.6.1
langfuse>=4.14.1
python-dotenv>=1.1.0
botocore>=1.43.40

# Dev / tests
pytest>=8
