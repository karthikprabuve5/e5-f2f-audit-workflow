[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "f2f-orchestration"
version = "0.1.0"
description = "Face-to-Face (F2F) / Plan-of-Care (POC) home-health audit agent orchestration."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Proprietary" }
authors = [{ name = "E5" }]
keywords = ["home-health", "f2f", "audit", "deepagents", "langfuse", "orchestration"]

dependencies = [
    "deepagents>=0.6.12",
    "langchain-aws>=1.6.1",
    "langfuse>=4.14.1",
    "python-dotenv>=1.1.0",
    "botocore>=1.43.40",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

# Convenience CLIs. `python -m f2f_orchestration.run_<x>` keeps working too.
[project.scripts]
f2f-poc = "f2f_orchestration.run_poc:main"
f2f-f2f = "f2f_orchestration.run_f2f:main"
f2f-merge = "f2f_orchestration.run_merge_encounters:main"
f2f-select = "f2f_orchestration.run_selection:main"
f2f-audit = "f2f_orchestration.run_audit:main"

[tool.setuptools.packages.find]
include = ["f2f_orchestration*"]
exclude = ["f2f_orchestration.tests*"]

# Ship the bundled prompts and skills so an installed wheel is self-contained
# (the agentic layer loads them at runtime). Runtime I/O dirs (ocr-markdown/,
# outputs/, soc_dates.json) are NOT packaged — they stay external and are
# overridable via OCR_MARKDOWN_DIR / OUTPUTS_DIR / SOC_DATES_FILE.
[tool.setuptools.package-data]
"f2f_orchestration" = ["prompts/**/*.md", "skills/**/*.md"]
